<h1 class="display-1 bg-dark text-white mb-2" >Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>
<% if user_signed_in? %>
  <h2>Welcome <%=current_user.email%></h2>
  <p class="text-primary">
    <%= current_user.role%>
  </p>
  <%= link_to 'Logout', destroy_user_session_path, data: {turbo_method: :delete, turbo_message: "Are you sure?"} %>
<% end %>
<% if user_signed_in? %>
  <div>
    <%= link_to 'Invite a friend',  new_user_invitation_path, class: "btn btn-primary" %>
  </div>
<% end %>
